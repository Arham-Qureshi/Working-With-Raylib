#pragma once

class Position{
    public:
        Position(int row,int column); //contructor -->takes input of row and colum or x and y plane value as (x,y)-->whose value will be one for filled.
        int row,column;
};